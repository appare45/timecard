import React, { ReactElement } from 'react';
import { GroupTemplate } from '../templates/group';
export const MemberPrint = (): ReactElement => (
  <GroupTemplate title="印刷">
    <></>
  </GroupTemplate>
);
